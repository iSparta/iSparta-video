var config = {
    /*每帧数据的宽度*/
    width: 750,
    /*每帧数据的高度*/
    height: 1334,
    /*强烈建议设置为0即可！不然或许会引起不必要的麻烦*/
    gap: 0,
    /*输出文件名*/
    outputName: 'output_s.png',
    outputType: 'png'// 默认的保存格式，不需要修改
};

module.exports = config;